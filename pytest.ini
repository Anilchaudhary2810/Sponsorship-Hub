[pytest]
testpaths = backend/tests
asyncio_mode = auto
addopts = --strict-markers --disable-warnings --cov=backend --cov-report=term-missing
python_files = test_*.py
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
